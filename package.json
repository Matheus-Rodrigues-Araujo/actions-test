{
  "name": "actions",
  "version": "1.0.0",
  "description": "tests with github actions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --transpile-only src/app.ts"
  },
  "author": "Matheus",
  "license": "ISC",
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "ts-prune": "^0.10.3",
    "typescript": "^5.5.4"
  }
}
